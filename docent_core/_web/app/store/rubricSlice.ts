// Deprecated import path kept temporarily for compatibility.
// Prefer importing from `@/app/types/rubricTypes`.
export type {
  ModelOption,
  Rubric,
  JudgeResult,
  JudgeResultWithCitations,
  RubricCentroid,
} from '@/app/types/rubricTypes';
