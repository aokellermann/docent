name: METR Deploy

on:
  push:
    branches: [metr]
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/deploy-reusable.yml
    with:
      deployment_id: metr
      github_environment: metr
      deploy_frontend_apprunner: true
      vercel_environment: Production
    secrets: inherit
